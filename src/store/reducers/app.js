// import { CURRENT_STEP, SOMETHING_ELSE } from '../../constants';

export default (state = {}, action) => {
    switch (action.type) {
        // case CURRENT_STEP:
        //     return {

        //     }   
        // case SOMETHING_ELSE:
        //     return {
        //         ...state
        //     }
        default:
            return state;
    }
}
